{
  "name": "infst-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply infst-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply infst-db --remote",
    "charts:sync:local": "node ./scripts/sync-charts-to-d1.mjs local",
    "charts:sync:remote": "node ./scripts/sync-charts-to-d1.mjs remote",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "drizzle-orm": "^0.38.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.65.0"
  }
}
